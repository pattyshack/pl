var i = 10
var i, var j = 10  // invalid

func foo() {}
func() {}  // invalid

{
  import "fmt@v1" // invalid

  1 + 2

  case 5: blah  // invalid

  stuff

  return

  unreachable * code

  more + unreachabele / code

  fallthrough

  s[i], s[j] = s[j], s[i]
}

var $(
  i = 5
  j int
  k int = 6
  (i, j) (int, string)
  (x, y) =  (1, 2)
  (x=x, y=y, ...) = foo()
)

{
  let $(
    x = s[1]
    y = s[2]
  )
}
