var _ enum(
  X string
  (Int)
  default X string
  default ()

  func bad()

  _ bad

  ok _
)

type X = enum(
  X string
  (Int)
  default X string
  default ()

  func bad()

  _ bad

  bad _

  bad [Int]
)
