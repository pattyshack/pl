var i = 10
var i, var j = 10  // invalid

func foo() {}
func() {}  // invalid

{
  import "fmt" // invalid

  1 + 2

  case 5: blah  // invalid

  stuff

  return

  unreachable * code

  more + unreachabele / code

  fallthrough

  s[i], s[j] = s[j], s[i]
}
